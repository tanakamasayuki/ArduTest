# Basic

Minimal ArduTest sketch.

The example defines two tests, emits a log line, reports one metric, and attaches one text artifact. The current library skeleton auto-runs all tests once from `loop()` so the sketch can be smoke-tested without the future `arduino_test` protocol.
